version: "3"
services:
  hello-world: 
    image: hello-world

  web:
    build: 
      context: ./
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports: 
      - "80:3000"